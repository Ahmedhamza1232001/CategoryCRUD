@model Category

<form method="post" asp-action="Delete">
    @*or you can left it alone
    it will uderstand becuase get and post have the same name*@
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Remove Category</h2>
        </div>

        @*<div asp-validation-summary="All"></div>*@
        <div class="mb-3">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" readonly /> <!--what is formcontrol class-->
        </div>

        <div class="mb-3">
            <label asp-for="Age">Age</label>
            <input asp-for="Age" class="form-control" readonly/>
        </div>

        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-controller="Category" asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</form>

